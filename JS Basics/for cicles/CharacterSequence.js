function solve(input) {
  let text = input.shift();
  let length = text.length;
  let sum = "";
  for (let i = 0; i < length; i++) {
    sum = text[i];
    console.log(sum);
  }
}
solve(["softuni"]);
