function solve(input){

    let name = input.shift();
    let needHours = Number(input.shift());
    let timeForProjects = needHours * 3;

    console.log(`The architect ${name} will need ${needHours} hours to complete ${timeForProjects} projects. `);


}
solve(["George",
"4"

]);