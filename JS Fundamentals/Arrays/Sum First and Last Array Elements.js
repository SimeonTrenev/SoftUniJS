function summFirstAndLastElement(array){

    let firstElement = Number(array[0])
    let lastElement = Number(array[array.length - 1])
    console.log(firstElement + lastElement)

}
summFirstAndLastElement(['20', '30', '40'])